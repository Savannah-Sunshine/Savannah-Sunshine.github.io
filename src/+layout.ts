// Will prerender all pages
export const prerender = true

// disable server side rendering and makes a SINGLE PAGE APP
export const ssr = false;

// will make sure pico is always there
// import "@picocss/pico/css/pico.css"
